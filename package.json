{
  "name": "gardenofwitches-wiki",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python3 scripts/generate.py && node scripts/prepare-deploy.mjs",
    "test": "node tests/verify.mjs",
    "serve": "node server.mjs"
  }
}
